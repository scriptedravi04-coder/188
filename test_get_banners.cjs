const axios = require('axios');
async function test() {
   try {
       // Since the user is logged in, they send cookies or Bearer token.
       // Let's just create a custom token or see if it's protected.
       // The endpoint requires admin role. I can't bypass auth easily via curl without token.
   } catch (e) {}
}
